# Cloudflare Pages bootstraps this project with npm before running the Bun build.
# The prebuild script restores the exact bun.lock dependency graph. This flag
# keeps Cloudflare's preliminary npm install from failing on the intentional
# Astro 6 + @astrojs/tailwind peer gap.
legacy-peer-deps=true
