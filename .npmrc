# Cloudflare Pages builds with npm even though this repo uses bun locally.
# bun resolves peers leniently; these flags make a clean `npm install` behave the
# same so CI doesn't fail on the (intentional) Astro 6 + @astrojs/tailwind peer gap.
legacy-peer-deps=true
